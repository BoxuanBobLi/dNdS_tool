# vgrG FASTA Files

This folder should contain the aligned `vgrG` gene FASTA files for **one condition** (e.g., acute or chronic).

The required files can be found in the workflow intermediate output directory using the structure:

```text
final_aln/<VFG_ID>.nogap.dedup/<VFG_ID>.nogap.dedup.nt_ali.fasta

